document.addEventListener("DOMContentLoaded", function(e) { 

  var width = 500;
  var height = 500;

  var canvas = d3.select('body')
              .append('svg')
              .attr('width',width)
              .attr('height',height);

});